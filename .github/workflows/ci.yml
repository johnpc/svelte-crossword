name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - run: npm ci

      - name: Create amplify_outputs stub for tests
        run: echo '{"custom":{"sqlQueriesFunctionName":"stub"}}' > src/amplify_outputs.json

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run check
        continue-on-error: true

      - name: Unit tests with coverage
        run: npm run test:coverage

      - name: CRAP score check
        run: node scripts/crap-check.js

      - name: Max line length check (150 lines)
        run: |
          FAILED=0
          for f in $(find src -name "*.svelte" -o -name "*.ts" -o -name "*.js" | grep -v node_modules | grep -v '.test.' | grep -v '.spec.'); do
            LINES=$(wc -l < "$f")
            if [ "$LINES" -gt 150 ]; then
              echo "FAIL: $f ($LINES lines, max 150)"
              FAILED=1
            fi
          done
          if [ "$FAILED" -eq 1 ]; then
            exit 1
          fi
          echo "All files within 150-line limit"

      - name: Build
        run: npm run build

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: E2E tests
        run: npm run test:e2e
        continue-on-error: true

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/
